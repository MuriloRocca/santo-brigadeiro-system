# Copie este arquivo para ".env" (sem o ".example") e ajuste os valores.
# O arquivo ".env" real NUNCA deve ir para o Git — adicione-o no .gitignore.

POSTGRES_DB=santo_brigadeiro
POSTGRES_USER=santo_brigadeiro_user
POSTGRES_PASSWORD=changeme_local_dev
